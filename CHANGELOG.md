# 更新日志

## v0.4.0 - 远程文件同步与缓存位置增强

发布日期：2026-05-23

### 新增

- 远程文件下载/同步改为客户端 Rust 流式传输，避免继续依赖 WebView 下载能力。
- 远程文件首次同步支持选择空文件夹作为目标位置。
- 已缓存的远程文件或文件夹支持变更缓存位置，并更新后续打开、同步使用的本地路径。
- 下载/同步过程增加后端进度事件，前端可持续展示传输进度。

### 调整

- 右键远程文件操作合并为“同步”入口；“同步到”仅在文件或文件夹尚未缓存时显示。
- 下载和同步复用同一个底层文件传输接口，减少行为差异。
- 远程文件名清理逻辑保留开头的点，避免 `.env` 等隐藏文件名被改成 `env`。

### 修复

- 修复大文件通过 WebView Blob/base64 下载时可能失败的问题。
- 修复浏览器下载可能将文件保存为 `.bin` 的问题。
- 修复失败下载可能留下不完整缓存文件的问题。
